# Additional DB Configurations
# ===
spring.jpa.hibernate.ddl-auto=update
logging.level.root=INFO
logging.level.org.springframework.security=DEBUG
logging.level.com.stemlink.skillmentor.security=DEBUG

# Authentication Configuration (Clerk)
# ===
auth.validator.type=clerk

# Prod
clerk.jwks.url=${CLERK_JWKS_URL:https://busy-sunbeam-61.clerk.accounts.dev/.well-known/jwks.json}

# CORS set to your Vercel frontend URL on Render once deployed
# e.g. CORS_ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:3001
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3001}

